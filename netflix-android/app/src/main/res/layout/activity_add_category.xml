<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background"
    android:padding="20dp">


    <!-- ✅ Page Title -->
    <TextView
        android:id="@+id/category_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Category"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary"
        android:layout_gravity="center"
        android:paddingTop="20dp"/>

    <!-- ✅ Category Name Input (Fixed) -->
    <EditText
        android:id="@+id/category_name_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:backgroundTint="@color/edittext_tint"
        android:padding="12dp"
        android:textColor="@color/text_primary"
        android:textSize="18sp" />

    <!-- ✅ Promoted / Not Promoted Button -->
    <Button
        android:id="@+id/button_promoted"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Not Promoted"
        android:textColor="@color/text_on_button"
        android:backgroundTint="@color/button_gray"
        android:layout_marginTop="20dp"/>

    <!-- ✅ Buttons Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="40dp">

        <!-- ✅ Cancel Button -->
        <Button
            android:id="@+id/button_cancel"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:text="Cancel"
            android:textColor="@color/text_on_button"
            android:backgroundTint="@color/button_gray"/>

        <!-- ✅ Submit Button -->
        <Button
            android:id="@+id/button_submit_category"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:text="Add Category"
            android:textColor="@color/text_on_button"
            android:backgroundTint="@color/button_red"
            android:layout_marginStart="20dp"/>
    </LinearLayout>

    <!-- ✅ Delete FAB -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/button_delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@android:drawable/ic_menu_delete"
        android:tint="@color/text_on_button"
        android:backgroundTint="@color/button_delete"
        android:layout_gravity="bottom|end"
        android:layout_margin="20dp"
        android:visibility="gone"/>
</LinearLayout>
